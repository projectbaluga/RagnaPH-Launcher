<Window x:Class="RagnaPHPatcher.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="RagnaPH Patcher"
        Height="500"
        Width="800"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None"
        ResizeMode="NoResize"
        MouseDown="Window_MouseDown">

    <Window.Background>
        <ImageBrush ImageSource="Images/background.png" Stretch="Fill" />
    </Window.Background>

    <Window.Resources>
        <!-- Red Button Style (Launch Game) -->
        <Style x:Key="RedButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#E53935"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Width" Value="160"/>
            <Setter Property="Height" Value="45"/>
            <Setter Property="Margin" Value="10"/>
            <Setter Property="HorizontalAlignment" Value="Left"/>
            <Setter Property="VerticalAlignment" Value="Bottom"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="Padding" Value="10"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="1" 
                                CornerRadius="10">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#D32F2F"/>
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Background" Value="#B71C1C"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Close Button Style -->
        <Style x:Key="CloseButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#FF1E88E5"/>
            <Setter Property="FontSize" Value="20"/>
            <Setter Property="Width" Value="35"/>
            <Setter Property="Height" Value="35"/>
            <Setter Property="HorizontalAlignment" Value="Right"/>
            <Setter Property="VerticalAlignment" Value="Top"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Content" Value="x"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Padding" Value="0"/>
            <Setter Property="Margin" Value="10"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="Transparent" BorderThickness="0">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Foreground" Value="Red"/>
                </Trigger>
            </Style.Triggers>
        </Style>

    </Window.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="4*"/>
            <ColumnDefinition Width="9*"/>
            <ColumnDefinition Width="387*"/>
        </Grid.ColumnDefinitions>
        <!-- News Section -->
        <GroupBox Header="News" Margin="313,30,10,62" FontFamily="Adobe Gothic Std B" FontSize="14" Foreground="#FF1E88E5" Grid.Column="2">
            <WebBrowser Name="NewsWebBrowser" />
        </GroupBox>

        <!-- Launch Game Button with Red Style -->
        <Button Name="LaunchGameButton"
                Style="{StaticResource RedButtonStyle}"
                Margin="2,0,0,10"
                Click="LaunchGameButton_Click" Grid.ColumnSpan="2" Grid.Column="1">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
                <Image Source="Images/launch_icon.png" Width="20" Height="20" Margin="5" />
                <TextBlock Text="Launch" VerticalAlignment="Center" Margin="5"/>
            </StackPanel>
        </Button>

        <!-- Close Button -->
        <Button Name="CloseButton"
                Style="{StaticResource CloseButtonStyle}"
                Click="CloseButton_Click"
                Margin="0,1,1,0" Grid.Column="2" />
        <Image Margin="63,12,540,380" Source="/Images/logo.png" Stretch="Fill" Grid.Column="2">
            <Image.Effect>
                <DropShadowEffect BlurRadius="1" Direction="210" Opacity="0.5" RenderingBias="Quality" ShadowDepth="5" Color="#FF737373"/>
            </Image.Effect>
        </Image>

    </Grid>
</Window>